<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nodes Information Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            padding: 20px;
            background-color: #f4f4f4;
        }
        h1 {
            color: #333;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th, td {
            padding: 10px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .header {
            font-size: 1.5em;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <h1>Nodes Information Report</h1>

    {% for host in groups['all'] %}
        <div class="host-info">
            <div class="header">Information for {{ host }}</div>
            <table>
                <tr><th>Fact</th><th>Value</th></tr>
                <tr><td>OS Family</td><td>{{ hostvars[host]['ansible_facts']['os_family'] }}</td></tr>
                <tr><td>IPv4 Address</td><td>
                    {% if hostvars[host]['ansible_facts']['network_interfaces'] is defined %}
                        {% for iface, details in hostvars[host]['ansible_facts']['network_interfaces'].items() %}
                            {% if 'ipv4' in details %}
                                {% for addr in details['ipv4'] %}
                                    {{ addr['address'] }}<br>
                                {% endfor %}
                            {% endif %}
                        {% endfor %}
                    {% else %}
                        Not available
                    {% endif %}
        
